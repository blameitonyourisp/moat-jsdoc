/**
 * @function
 * 
 * @moat {t_feature|p_0|s_open|b_master}
 * EI3-491
 * Temporary ticket to be deleted
 * This function needs a return statement
 */
function temp() {
    // this is a temporary file that will later be deleted
}